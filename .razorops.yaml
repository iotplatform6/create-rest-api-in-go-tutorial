global:
  runner: ubuntu
tasks:
  build-image:
    steps:
      - docker-build:
        image: goapi
        # push: true
        tags: ["latest"]
        dockerfile: Dockerfile
        context: .
      - docker-push:
          image: dockerhub/goapi
          tags: ["latest"]
