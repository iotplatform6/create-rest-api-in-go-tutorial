global:
  runner: razorci/base
tasks:
  build-image:
    steps:
      - docker-build:
        image: dockerhub/go_api
        # push: true
        tags: ["latest"]
        dockerfile: Dockerfile
        context: .
      - docker-push:
          image: dockerhub/go_api
          tags: ["latest"]
