global:
  runner: ubuntu
tasks:
  build-image:
    steps:
    - docker-build:
        image: goapi
        tags: ["latest"]
        dockerfile: Dockerfile
        context: .
    - docker-push:
        image: dockerhub/goapi
        ags: ["latest"]
